# Setting up react-testing-library(+jest) with Parcel
 - react-testing-library
 - jest
 - babel dependencies (for babel)
 - configure babel (by default it's setup for parcel)
 - jest configuration (npx jest --init)
 - jsdom